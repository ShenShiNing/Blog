import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Portfolio Detail",
};

const Page = () => {
  return <div className="main">Portfolio Detail</div>;
};

export default Page;
